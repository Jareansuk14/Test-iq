{
  "name": "iq-option-test",
  "version": "1.0.0",
  "description": "Simple IQ Option API Test",
  "main": "server.js",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "start": "node server.js",
    "postinstall": "pip install -r requirements.txt",
    "test": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "dotenv": "^16.0.3"
  }
}